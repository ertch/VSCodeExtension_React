---
interface Props {
	legend: string;
    id?: string;
    klasse?: string;
    gruppe?: string;
}

const {legend, klasse, id, gruppe} = Astro.props;
---

<fieldset id={id} class={klasse as 'text'} data-grp={gruppe}>
    <legend>{legend}</legend>
    <slot />
</fieldset>