---
interface Props {
	title: string;
    id?: string;
    klasse?: string;
}

const {title, klasse, id} = Astro.props;
---

<fieldset id={id} class={klasse as 'text'}>
    <legend>{title}</legend>
    <slot />
</fieldset>