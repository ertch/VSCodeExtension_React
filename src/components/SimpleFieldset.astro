---
interface Props {
	legend: string;
    id?: string;
    klasse?: string;
}

const {legend, klasse, id} = Astro.props;
---

<fieldset id={id} class={klasse as 'text'}>
    <legend>{legend}</legend>
    <slot />
</fieldset>