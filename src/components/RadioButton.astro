---
interface Props {
  id: string;
  name: string;
  required?: boolean;
  value: string;
  label: string;
}

const { id, name, required, value, label } = Astro.props;
---

<div class="radio-option">
  <input class="radio-option__input" type="radio" id={id} name={name} value={value} {required} data-required = 'false';/>
  <label class="radio-option__label" for={id}>{label}</label>
</div>