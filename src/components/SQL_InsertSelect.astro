---
interface Props {
  id: string;
  label: string;
  name: string;
  required?: boolean;
  call?: string;
}

const { id, label, name, required, call } = Astro.props;
let errorname = id.replace(/^([^_]*)_/, "$1_error_");
---

<label for={id}>{label}</label>
<span class="errormessage" id={errorname}></span>
<select class="dropdown h-drop" id={id} name={name} {required} onchange={call}>
  <option disabled value="" selected>[Bitte Auswählen]</option>
</select>
